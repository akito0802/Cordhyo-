(()=>{'use strict';
function enhance(pack){if(!pack||pack.dataset.tabReady)return;const boxes=[...pack.querySelectorAll('.theory-study-box')];if(boxes.length<2)return;pack.dataset.tabReady='1';const nav=document.createElement('div');nav.className='theory-study-tabs';nav.setAttribute('role','tablist');const labels=boxes.map((box,i)=>{const h=box.querySelector('h5');return h?h.textContent.trim():`項目${i+1}`});boxes.forEach((box,i)=>{const id=`study-panel-${Math.random().toString(36).slice(2)}-${i}`;box.id=id;box.classList.add('theory-study-panel');box.hidden=i!==0;box.setAttribute('role','tabpanel');const btn=document.createElement('button');btn.type='button';btn.className='theory-study-tab'+(i===0?' active':'');btn.textContent=labels[i];btn.setAttribute('role','tab');btn.setAttribute('aria-controls',id);btn.setAttribute('aria-selected',i===0?'true':'false');btn.onclick=()=>{nav.querySelectorAll('.theory-study-tab').forEach((b,n)=>{const active=n===i;b.classList.toggle('active',active);b.setAttribute('aria-selected',active?'true':'false');boxes[n].hidden=!active});};nav.appendChild(btn)});const grid=pack.querySelector('.theory-study-grid');grid.before(nav);grid.classList.add('theory-study-single')}
function scan(){document.querySelectorAll('.theory-study-pack').forEach(enhance)}
function init(){scan();const root=document.getElementById('theoryCards');if(root)new MutationObserver(scan).observe(root,{childList:true,subtree:true})}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();
})();